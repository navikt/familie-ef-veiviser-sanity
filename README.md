# familie-ef-veiviser-sanity

## Deploye

Deployes manuelt ved merge til main

### Installere Sanity

- Kjør `npm install -g @sanity/cli` i terminalen.

### Kjøre lokalt

- Kjør `npm ci` for å installere alle npm-pakkene.
- Kjør `npm run dev` for å starte applikasjonen på `http://localhost:3333/`.
